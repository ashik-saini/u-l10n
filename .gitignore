# Binaries
/u-l10n
/bin/

# Test and coverage output
/reports/
*.out
*.test

# Local environment overrides — never commit real credentials
.env
.env.local
# Generated by `make s3-cred-local`: the placeholder credential file storage/v4
# refuses to start without. Ignored so a real one can never be committed here.
/.local/

# Editor / OS
.DS_Store
.idea/
.vscode/
# Added by code-review-graph
.code-review-graph/
